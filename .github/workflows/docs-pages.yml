name: Docs Pages

on:
  push:
    branches: [main]
    paths:
      - docs/**
      - mkdocs.yml
      - docs/requirements.txt
      - README.md
      - scripts/install-gui.sh
      - .github/workflows/docs-pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up uv
        uses: astral-sh/setup-uv@v7

      - name: Stage GUI install script
        run: cp scripts/install-gui.sh docs/install-gui.sh

      - name: Build docs
        run: uv run --with-requirements docs/requirements.txt zensical build --clean

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
        with:
          timeout: 1800000 # 30 minutes in milliseconds to accommodate Pages queue delays
